<main class="flow-shell" data-pagefind-body>
  <section class="flow-grid" aria-label="Gou Min 的个人站">
    <section class="flow-column main-column">
      <slot name="intro" />
      <slot name="featured" />
      <slot name="doing" />
    </section>

    <aside class="flow-column side-column">
      <slot name="profile" />
      <slot name="latest" />
      <slot name="navigation" />
      <slot name="connect" />
    </aside>
  </section>
</main>

<style>
  .flow-shell {
    width: min(100%, var(--layout-shell-width));
    margin: 0 auto;
    padding: 132px var(--layout-page-gutter) var(--layout-page-bottom);
  }

  .flow-grid {
    display: grid;
    grid-template-columns: minmax(0, 720px) minmax(264px, 304px);
    justify-content: center;
    gap: 64px;
    align-items: start;
  }

  .flow-column {
    display: grid;
    align-content: start;
    min-width: 0;
  }

  .flow-grid :global(.glass-card) {
    width: 100%;
  }

  .flow-grid :global(.profile-card) {
    min-height: auto;
    margin-bottom: 20px;
  }

  .flow-grid :global(.heatmap-card) {
    padding: 18px 0;
  }

  .flow-grid :global(.nav-card) {
    padding: 2px 0 0;
  }

  .flow-grid :global(.connect-card) {
    padding: 14px 0 0;
  }

  .flow-grid :global(.intro-card) {
    min-height: 0;
    padding: 0 0 8px;
  }

  .flow-grid :global(.featured-card) {
    margin-top: 34px;
  }

  @media (max-width: 920px) {
    .flow-grid {
      grid-template-columns: 1fr;
      gap: 26px;
    }

    .side-column {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 820px) {
    .flow-shell {
      padding: 96px 18px 42px;
    }

    .flow-grid {
      display: flex;
      flex-direction: column;
      gap: 24px;
    }

    .main-column,
    .side-column {
      display: contents;
    }

    .flow-grid :global(.intro-card) {
      min-height: auto;
      padding: 0;
    }

    .flow-grid :global(.connect-card) {
      margin-block: 0;
    }

    .flow-grid :global(.heatmap-card) {
      margin-top: 0;
    }

    .flow-grid :global(.featured-card) {
      margin-top: 0;
    }
  }
</style>
